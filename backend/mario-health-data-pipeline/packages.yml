packages:
  # dbt utilities - helpful macros and tests
  - package: dbt-labs/dbt_utils
    version: 1.1.1
  
  # Data quality tests (optional but recommended)
  # - package: calogica/dbt_expectations
  #   version: 0.10.1
  
  # Audit helper (optional - useful for comparing model versions)
  # - package: dbt-labs/audit_helper
  #   version: 0.9.0

# After adding packages, run: dbt deps

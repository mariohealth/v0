These are example JSONs that our API will return as you query different endpoints:

- `categories.json`
	- is the JSON returned by the `GET /api/categories` endpoint
	- it lists all the categories covered by Mario Health

